# Mizan Web — environment template.
# Copy to .env.local and fill in real values. NEVER commit .env.local.

# Railway API URL — the deployed Node.js/Express backend
VITE_API_URL=http://localhost:4000
